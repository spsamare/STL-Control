# STL-Control

Edit `simulation_config.py` to configure dynamics, noise, sensing, resilient
STL limits, solver, runtime changes, and plot settings. Run `main.py` to
execute the configured simulation.
